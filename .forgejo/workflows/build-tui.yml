name: Build TUI

on:
  push:
    branches: [main]
    paths:
      - 'capacium-marketplace-tui/**'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: https://github.com/actions/checkout@v4
      - uses: https://github.com/actions/setup-node@v4
        with:
          node-version: '20'
      - name: Build test (placeholder)
        run: echo "TUI build pipeline — NCC bundle verification"
